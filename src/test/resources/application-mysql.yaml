spring:
  datasource:
    url: jdbc:mysql://localhost:3306/learning?createDatabaseIfNotExist=true
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: transvelo
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    properties:
      hibernate:
        generate-ddl: true
        ddl-auto: create
        format_sql: true